package com.badlogic.drop;
/** ^^ rename the package as you have named it via Libgdx setup

import com.badlogic.gdx.game;
import com.badlogic.gdx.Gdx;
import com.kilobolt.screens.GameScreen; //look at the next file



public class Game2016 extends Game {
    
    
    @Override
    public void create() {
        Gdx.app.log("/file/name", "created"); //Acts as System.out.println()
        setScreen(new GameScreen());
    }
    
}
